import 'package:flutter/cupertino.dart';

class Footer extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Made with ❤️ by Taiyr Begeyev',
      ),
    );
  }
}
